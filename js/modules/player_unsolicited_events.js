(function(Player) {
    'use strict';

    var player;
    var DAILY_CHANCE = 0.02;
    var ENTRY_CHANCE = 0.05;
    var GLOBAL_COOLDOWN_DAYS = 3;
    var PER_EVENT_COOLDOWN_DAYS = 60;
    var MAX_ACTIVE_MULTI_STEP = 3;
    var ACTIVE_EXPIRY_DAYS = 30;
    var CATEGORY_COOLDOWNS = {
        common: 2,
        trade: 3,
        social: 5,
        crime: 4,
        war: 3,
        political: 5,
        supernatural: 14,
        skill: 7,
        rank: 7,
        context: 3
    };
    var MULTI_TEMPLATES = {
        delayed_notice: true,
        trade_chain: true,
        romance_chain: true,
        investigation_chain: true,
        war_chain: true,
        political_chain: true,
        rank_chain: true,
        mystic_chain: true,
        skill_chain: true,
        context_chain: true,
        long_omen: true
    };
    var FOOD_IDS = ['wheat', 'bread', 'meat', 'fish', 'vegetables', 'eggs', 'honey', 'poultry', 'grapes'];
    var TRADE_IDS = ['bread', 'ale', 'wine', 'herbs', 'wood', 'planks', 'stone', 'bricks', 'iron', 'steel', 'tools', 'cloth', 'silk', 'wool', 'jewelry', 'bandages', 'rope', 'leather', 'hemp', 'hide', 'charcoal', 'perfume', 'pearls', 'furniture', 'saddles'];
    var WAR_IDS = ['swords', 'armor', 'bows', 'arrows', 'bandages', 'horses', 'iron', 'steel'];
    var LUXURY_IDS = ['wine', 'silk', 'jewelry', 'perfume', 'fine_clothes', 'tapestry', 'gold_goblet', 'pearl_jewelry', 'pearls'];
    var CRAFT_IDS = ['wood', 'planks', 'stone', 'bricks', 'clay', 'iron_ore', 'iron', 'steel', 'tools', 'leather', 'cloth', 'wool', 'rope', 'hemp', 'charcoal', 'hide'];
    var RESOURCE_NAMES = {
        wheat: 'wheat', bread: 'bread', meat: 'meat', fish: 'fish', vegetables: 'vegetables', eggs: 'eggs', honey: 'honey', salt: 'salt', ale: 'ale', wine: 'wine', herbs: 'herbs', wood: 'wood', planks: 'planks', stone: 'stone', bricks: 'bricks', clay: 'clay', iron_ore: 'iron ore', iron: 'iron', steel: 'steel', tools: 'tools', leather: 'leather', cloth: 'cloth', silk: 'silk', wool: 'wool', jewelry: 'jewelry', swords: 'swords', armor: 'armor', bows: 'bows', arrows: 'arrows', bandages: 'bandages', horses: 'horses', rope: 'rope', hemp: 'hemp', charcoal: 'charcoal', hide: 'hide', perfume: 'perfume', fine_clothes: 'fine clothes', tapestry: 'tapestry', gold_goblet: 'gold goblet', pearl_jewelry: 'pearl jewelry', pearls: 'pearls', poultry: 'poultry', grapes: 'grapes', furniture: 'furniture', saddles: 'saddles', gut_string: 'gut string'
    };
    var SPECIAL_TITLES = {
        lords_hunt: "Lord's Hunt",
        kings_midnight_walk: "King's Midnight Walk",
        merchant_prince_offer: 'Merchant-Prince Offer',
        royal_advisor_whisper: 'Royal Advisor Whisper',
        crown_burden: 'Crown Burden',
        throne_room_echo: 'Throne Room Echo',
        judge_in_disguise: 'Judge in Disguise',
        secret_admirer_letter: 'Secret Admirer Letter',
        witness_protection: 'Witness Protection'
    };

    function _sync() {
        player = Player && Player.state ? Player.state : null;
        return player;
    }
    function _getDay() { try { return Engine.getDay(); } catch (e) { return 0; } }
    function _getRng() { try { return Engine.getRng(); } catch (e) { return null; } }
    function _getWorld() { try { return Engine.getWorld(); } catch (e) { return null; } }
    function _findTown(id) { try { return id ? Engine.findTown(id) : null; } catch (e) { return null; } }
    function _findPerson(id) { try { return id ? Engine.findPerson(id) : null; } catch (e) { return null; } }
    function _findKingdom(id) { try { return id ? Engine.findKingdom(id) : null; } catch (e) { return null; } }
    function _log(msg, details, category) { try { Engine.logEvent(msg, details || null, category || 'my_actions'); } catch (e) {} }

    function _ensureState() {
        _sync();
        if (!player) return null;
        if (player._pendingUnsolicitedEvent === undefined) player._pendingUnsolicitedEvent = null;
        if (!player._activeUnsolicitedEvents) player._activeUnsolicitedEvents = [];
        if (!player._unsolicitedEventCooldowns) player._unsolicitedEventCooldowns = {};
        if (player._lastUnsolicitedEventDay == null) player._lastUnsolicitedEventDay = -9999;
        if (player._lastUnsolicitedEventEntryDay == null) player._lastUnsolicitedEventEntryDay = -9999;
        if (!player._nextUnsolicitedEventId) player._nextUnsolicitedEventId = 1;
        if (!player._unsolicitedEventCatCooldowns) player._unsolicitedEventCatCooldowns = {};
        if (!player.inventory) player.inventory = {};
        if (!player.skills) player.skills = {};
        if (!player.achievements) player.achievements = {};
        if (!player.socialRank) player.socialRank = {};
        if (!player.criminalRecord) player.criminalRecord = {};
        if (!player.childrenIds) player.childrenIds = [];
        if (!player.relationships) player.relationships = {};
        if (!player.buildings) player.buildings = [];
        if (!player.illnesses) player.illnesses = [];
        if (!player.injuries) player.injuries = [];
        return player;
    }

    function _isTutorial() {
        try { return typeof Tutorial !== 'undefined' && Tutorial.isActive && Tutorial.isActive(); } catch (e) { return false; }
    }
    function _isStorySuppressed() {
        return !!(player && player.storyMode && player.storyMode.active && !player.storyMode.complete);
    }
    function _isEncounterSuppressed() {
        return !!(player && player.storyMode && player.storyMode.flags && player.storyMode.flags.suppressEncounters);
    }
    function _suppressed() {
        return _isTutorial() || _isStorySuppressed() || _isEncounterSuppressed();
    }
    function _num(value, fallback) {
        return typeof value === 'number' && !isNaN(value) ? value : fallback;
    }
    function _copy(obj) {
        return obj ? JSON.parse(JSON.stringify(obj)) : obj;
    }
    function _countTruthy(obj) {
        var count = 0;
        var k;
        if (!obj) return 0;
        for (k in obj) if (obj[k]) count++;
        return count;
    }
    function _countInventory(ids) {
        var total = 0;
        var i;
        for (i = 0; i < ids.length; i++) total += player.inventory[ids[i]] || 0;
        return total;
    }
    function _countCrimes() {
        var total = 0;
        var kid, crimeMap, crimeId;
        for (kid in player.criminalRecord) {
            crimeMap = player.criminalRecord[kid] || {};
            for (crimeId in crimeMap) total += crimeMap[crimeId] || 0;
        }
        return total;
    }
    function _playerName() {
        return player && player.firstName ? player.firstName : 'Merchant';
    }
    function _kingdomAtWar(kingdom) {
        if (!kingdom || !kingdom.atWar) return false;
        return Array.isArray(kingdom.atWar) ? kingdom.atWar.length > 0 : !!kingdom.atWar.size;
    }
    function _currentKingdomId() {
        if (player && player.isKing && player.kingState && player.kingState.kingdomId) return player.kingState.kingdomId;
        if (player && player.townId) {
            var t = _findTown(player.townId);
            if (t && t.kingdomId) return t.kingdomId;
        }
        return null;
    }
    function _playerRankIndex(kid) {
        if (!kid) kid = _currentKingdomId();
        return kid && player.socialRank ? (player.socialRank[kid] || 0) : 0;
    }
    function _activeMultiCount() {
        var count = 0;
        var i, def, inst;
        for (i = 0; i < player._activeUnsolicitedEvents.length; i++) {
            inst = player._activeUnsolicitedEvents[i];
            def = inst ? EVENT_DEF_MAP[inst.defId] : null;
            if (def && MULTI_TEMPLATES[def.template]) count++;
        }
        return count;
    }
    function _buildContext() {
        var day = _getDay();
        var rng = _getRng();
        var world = _getWorld();
        var town = player && player.townId ? _findTown(player.townId) : null;
        var kid = town && town.kingdomId ? town.kingdomId : _currentKingdomId();
        var kingdom = kid ? _findKingdom(kid) : null;
        return {
            player: player,
            day: day,
            rng: rng,
            world: world,
            town: town,
            kingdom: kingdom,
            kingdomId: kid,
            townName: town ? town.name : 'the road',
            kingdomName: kingdom ? kingdom.name : 'the realm',
            playerName: _playerName(),
            security: _num(town && town.security, 50),
            happiness: _num(town && town.happiness, 50),
            prosperity: _num(town && town.prosperity, 50),
            atWar: _kingdomAtWar(kingdom),
            foodCount: _countInventory(FOOD_IDS),
            tradeCount: _countInventory(TRADE_IDS),
            skillCount: _countTruthy(player.skills),
            achievementCount: _countTruthy(player.achievements),
            crimeCount: _countCrimes(),
            activeMulti: _activeMultiCount()
        };
    }

    function _titleize(id) {
        var title, parts, i;
        if (SPECIAL_TITLES[id]) return SPECIAL_TITLES[id];
        parts = (id || '').split('_');
        for (i = 0; i < parts.length; i++) {
            if (!parts[i]) continue;
            parts[i] = parts[i].charAt(0).toUpperCase() + parts[i].slice(1);
        }
        title = parts.join(' ');
        return title || id;
    }
    function _derivedExtra(def) {
        var extra = _copy(def.extra || {});
        var id = def.id;
        if (!extra.npcRole) {
            if (def.category === 'trade') extra.npcRole = 'merchant';
            else if (def.category === 'social') extra.npcRole = (id.indexOf('noble') >= 0 || id.indexOf('court') >= 0 || id.indexOf('widow') >= 0 || id.indexOf('wedding') >= 0 || id.indexOf('patron') >= 0) ? 'noble' : 'town';
            else if (def.category === 'crime') extra.npcRole = (id.indexOf('guard') >= 0 || id.indexOf('watch') >= 0 || id.indexOf('warrant') >= 0) ? 'guard' : 'rogue';
            else if (def.category === 'war') extra.npcRole = 'soldier';
            else if (def.category === 'political') extra.npcRole = (id.indexOf('royal') >= 0 || id.indexOf('crown') >= 0 || id.indexOf('palace') >= 0) ? 'king' : 'noble';
            else if (def.category === 'rank') extra.npcRole = (id.indexOf('king') >= 0 || id.indexOf('crown') >= 0 || id.indexOf('throne') >= 0 || id.indexOf('royal') >= 0) ? 'king' : 'noble';
            else extra.npcRole = 'town';
        }
        if (def.category === 'war') extra.war = true;
        if (def.category === 'skill' && extra.skillCountAtLeast == null) extra.skillCountAtLeast = 1;
        if (id.indexOf('quarantine') >= 0 || id.indexOf('plague') >= 0) extra.townQuarantined = true;
        if (id === 'low_security_bounty') extra.securityBelow = 45;
        if (id === 'high_security_request') extra.securityAbove = 60;
        if (id === 'prosperous_patron' || id === 'luxury_mania' || id === 'merchant_prince_offer') extra.wealthAbove = 500;
        if (id === 'famine_kitchen' || id === 'cheap_bread_riot' || id === 'empty_wells' || id === 'winter_shortage' || id === 'drought_omen') extra.foodLow = true;
        if (id === 'overflowing_granary' || id === 'harvest_blessing') extra.foodPlenty = true;
        if (id === 'tea_with_widow' || id === 'secret_admirer_letter' || id === 'balcony_serenade' || id === 'moonlit_confession' || id === 'winter_courtship' || id === 'noble_flirtation') extra.noSpouse = true;
        if (id === 'child_delivers_flower') extra.hasChildren = true;
        if (id === 'lover_quarrel') extra.hasSpouse = true;
        if (id === 'crown_burden' || id === 'kings_midnight_walk') extra.isKing = true;
        if (id === 'royal_advisor_whisper' || id === 'throne_room_echo') extra.rankAtLeast = 6;
        if (id === 'lords_hunt' || id === 'vassal_plea') extra.rankAtLeast = 5;
        if (id === 'minor_noble_request' || id === 'title_challenge' || id === 'heralds_bow') extra.rankAtLeast = 4;
        if (id === 'guildmaster_notice') extra.rankAtLeast = 3;
        if (id === 'burgher_invitation') extra.rankAtLeast = 2;
        if (id === 'citizen_dispute') extra.rankAtLeast = 1;
        if (id === 'peasant_petition') extra.rankAtMost = 1;
        if (id === 'notorious_reputation') extra.crimeHistory = true;
        if (id === 'pickpocket_brush' || id === 'tavern_brawl_bet' || id === 'bridge_collapse' || id === 'flood_salvage' || id === 'battlefield_relic' || id === 'grave_robber_tip' || id === 'collapsed_awning') extra.hazard = true;
        return extra;
    }

    function _peopleInTown(townId) {
        var world = _getWorld();
        var people = world && world.people ? world.people : [];
        var list = [];
        var i, p;
        for (i = 0; i < people.length; i++) {
            p = people[i];
            if (!p || p.alive === false || p.id === 'player') continue;
            if (townId && p.townId !== townId) continue;
            list.push(p);
        }
        return list;
    }
    function _pickNpc(ctx, role) {
        var rng = ctx.rng;
        var people = _peopleInTown(ctx.town ? ctx.town.id : null);
        var list = [];
        var i, p, rank;
        if (!rng || !people.length) return null;
        if (role === 'family') {
            if (player.spouseId) {
                p = _findPerson(player.spouseId);
                if (p && p.alive !== false && (!ctx.town || p.townId === ctx.town.id)) list.push(p);
            }
            for (i = 0; i < player.childrenIds.length; i++) {
                p = _findPerson(player.childrenIds[i]);
                if (p && p.alive !== false && (!ctx.town || p.townId === ctx.town.id)) list.push(p);
            }
            return list.length ? list[rng.randInt(0, list.length - 1)] : null;
        }
        for (i = 0; i < people.length; i++) {
            p = people[i];
            rank = ctx.kingdomId && p.socialRank ? (p.socialRank[ctx.kingdomId] || 0) : 0;
            if (role === 'noble' && rank < 4) continue;
            if (role === 'king' && !((ctx.kingdom && ctx.kingdom.king === p.id) || rank >= 6)) continue;
            list.push(p);
        }
        if (role === 'king' && ctx.kingdom && ctx.kingdom.king) {
            p = _findPerson(ctx.kingdom.king);
            if (p && p.alive !== false) list.push(p);
        }
        if (!list.length) list = people.slice();
        return list.length ? list[rng.randInt(0, list.length - 1)] : null;
    }

    function _chooseFromGroup(def, ctx) {
        var rng = ctx.rng;
        var id = def.id;
        var extra = _derivedExtra(def);
        if (extra.resourceId) return extra.resourceId;
        if (id.indexOf('fish') >= 0) return 'fish';
        if (id.indexOf('grain') >= 0) return 'wheat';
        if (id.indexOf('silk') >= 0) return 'silk';
        if (id.indexOf('herb') >= 0) return 'herbs';
        if (id.indexOf('spice') >= 0) return 'perfume';
        if (id.indexOf('wine') >= 0) return 'wine';
        if (id.indexOf('candle') >= 0) return 'rope';
        if (id.indexOf('salt') >= 0) return 'salt';
        if (id.indexOf('horse') >= 0) return 'horses';
        if (id.indexOf('paper') >= 0 || id.indexOf('scribe') >= 0) return 'charcoal';
        if (id.indexOf('iron') >= 0) return 'iron';
        if (id.indexOf('tool') >= 0) return 'tools';
        if (id.indexOf('wood') >= 0 || id.indexOf('wagon') >= 0 || id.indexOf('bridge') >= 0) return 'wood';
        if (def.category === 'war') return WAR_IDS[rng.randInt(0, WAR_IDS.length - 1)];
        if (def.category === 'trade') return TRADE_IDS[rng.randInt(0, TRADE_IDS.length - 1)];
        if (def.category === 'context' && (id.indexOf('bread') >= 0 || id.indexOf('harvest') >= 0 || id.indexOf('granary') >= 0)) return FOOD_IDS[rng.randInt(0, FOOD_IDS.length - 1)];
        if (def.category === 'political' || def.category === 'rank') return LUXURY_IDS[rng.randInt(0, LUXURY_IDS.length - 1)];
        if (def.category === 'skill') return CRAFT_IDS[rng.randInt(0, CRAFT_IDS.length - 1)];
        return TRADE_IDS[rng.randInt(0, TRADE_IDS.length - 1)];
    }
    function _rarityBase(rarity) {
        if (rarity === 'legendary') return { gold: [120, 240], rep: [6, 10], rel: [5, 9], energy: [5, 10], qty: [5, 9], wait: [4, 8] };
        if (rarity === 'epic') return { gold: [80, 160], rep: [4, 8], rel: [4, 7], energy: [4, 8], qty: [4, 8], wait: [3, 6] };
        if (rarity === 'rare') return { gold: [45, 95], rep: [3, 6], rel: [3, 6], energy: [3, 6], qty: [3, 6], wait: [2, 5] };
        if (rarity === 'uncommon') return { gold: [20, 55], rep: [2, 4], rel: [2, 5], energy: [2, 5], qty: [2, 5], wait: [2, 4] };
        return { gold: [8, 28], rep: [1, 3], rel: [1, 4], energy: [1, 4], qty: [1, 4], wait: [1, 3] };
    }
    function _roll(rng, range) {
        return rng.randInt(range[0], range[1]);
    }
    function _passesCondition(ctx, def) {
        var extra = _derivedExtra(def);
        if (!ctx || !ctx.rng || !player || !ctx.town) return false;
        if (extra.war && !ctx.atWar) return false;
        if (extra.peace && ctx.atWar) return false;
        if (extra.townQuarantined && !(ctx.town && ctx.town.isQuarantined)) return false;
        if (extra.notQuarantined && ctx.town && ctx.town.isQuarantined) return false;
        if (extra.securityBelow != null && !(ctx.security < extra.securityBelow)) return false;
        if (extra.securityAbove != null && !(ctx.security > extra.securityAbove)) return false;
        if (extra.happinessBelow != null && !(ctx.happiness < extra.happinessBelow)) return false;
        if (extra.happinessAbove != null && !(ctx.happiness > extra.happinessAbove)) return false;
        if (extra.prosperityBelow != null && !(ctx.prosperity < extra.prosperityBelow)) return false;
        if (extra.prosperityAbove != null && !(ctx.prosperity > extra.prosperityAbove)) return false;
        if (extra.wealthAbove != null && !((player.gold || 0) >= extra.wealthAbove)) return false;
        if (extra.wealthBelow != null && !((player.gold || 0) <= extra.wealthBelow)) return false;
        if (extra.energyBelow != null && !((player.energy || 0) <= extra.energyBelow)) return false;
        if (extra.energyAbove != null && !((player.energy || 0) >= extra.energyAbove)) return false;
        if (extra.isKing && !(player.isKing && player.kingState && player.kingState.kingdomId)) return false;
        if (extra.notKing && player.isKing) return false;
        if (extra.hasSpouse && !player.spouseId) return false;
        if (extra.noSpouse && player.spouseId) return false;
        if (extra.hasChildren && !(player.childrenIds && player.childrenIds.length)) return false;
        if (extra.noChildren && player.childrenIds && player.childrenIds.length) return false;
        if (extra.rankAtLeast != null && !(_playerRankIndex(ctx.kingdomId) >= extra.rankAtLeast)) return false;
        if (extra.rankAtMost != null && !(_playerRankIndex(ctx.kingdomId) <= extra.rankAtMost)) return false;
        if (extra.skillCountAtLeast != null && !(ctx.skillCount >= extra.skillCountAtLeast)) return false;
        if (extra.achievementCountAtLeast != null && !(ctx.achievementCount >= extra.achievementCountAtLeast)) return false;
        if (extra.crimeHistory && !(ctx.crimeCount > 0)) return false;
        if (extra.foodLow && !(ctx.foodCount <= 6)) return false;
        if (extra.foodPlenty && !(ctx.foodCount >= 15)) return false;
        if (extra.injured && !(player.injuries && player.injuries.length)) return false;
        if (extra.ill && !(player.illnesses && player.illnesses.length)) return false;
        if (extra.buildingCountAtLeast != null && !((player.buildings || []).length >= extra.buildingCountAtLeast)) return false;
        if (extra.npcRole && !_pickNpc(ctx, extra.npcRole)) return false;
        return true;
    }
    function _fill(text, params, ctx) {
        var out = text || '';
        var k;
        var replacements = {
            townName: (params && params.townName) || (ctx && ctx.townName) || 'the town',
            kingdomName: (params && params.kingdomName) || (ctx && ctx.kingdomName) || 'the realm',
            playerName: (params && params.playerName) || (ctx && ctx.playerName) || 'Merchant',
            npcName: (params && params.npcName) || 'a stranger'
        };
        for (k in replacements) out = out.replace(new RegExp('\\{' + k + '\\}', 'g'), replacements[k]);
        if (params) {
            for (k in params) out = out.replace(new RegExp('\\{' + k + '\\}', 'g'), params[k]);
        }
        return out;
    }
    function _stepDelay(def, instance, nextStepIndex) {
        if (nextStepIndex == null) return 0;
        if (def.template === 'long_omen') {
            if (nextStepIndex === 1) return instance.params.waitDays;
            if (nextStepIndex === 2) return instance.params.waitDays2;
            if (nextStepIndex === 4) return 1;
            return 0;
        }
        if (MULTI_TEMPLATES[def.template] && nextStepIndex > 0) return instance.params.waitDays;
        return 0;
    }
    function _sceneLead(def) {
        if (def.category === 'trade') return def.title + ' ripples through {townName} like a rumor with teeth.';
        if (def.category === 'social') return def.title + ' arrives with a blush, a laugh, or a dangerous pause.';
        if (def.category === 'crime') return def.title + ' comes wrapped in shadow and bad ideas.';
        if (def.category === 'war') return def.title + ' blows in on the taste of smoke.';
        if (def.category === 'political') return def.title + ' slips beneath the doors of power.';
        if (def.category === 'supernatural') return def.title + ' makes the air feel older than it should.';
        if (def.category === 'skill') return def.title + ' beckons like a test set by a patient master.';
        if (def.category === 'rank') return def.title + ' reminds everyone which rung of the ladder you stand upon.';
        if (def.category === 'context') return def.title + ' grows straight out of {townName}\'s present troubles.';
        return def.title + ' interrupts the ordinary clatter of {townName}.';
    }
    function _rawStep(def, instance, ctx) {
        var step = instance.stepIndex || 0;
        var lead = _sceneLead(def);
        if (def.template === 'windfall') {
            return { title: def.title, icon: def.icon, text: lead + ' A little fortune lies within reach, and fate waits to see what kind of soul {playerName} will prove to be.', choices: [
                { id: 'pocket', label: 'Pocket the good fortune', effectKey: 'pocket' },
                { id: 'share', label: 'Ask around for the rightful owner', effectKey: 'share' },
                { id: 'leave', label: 'Leave it to fate and walk on', effectKey: 'leave' }
            ] };
        }
        if (def.template === 'aid') {
            return { title: def.title, icon: def.icon, text: lead + ' {npcName} needs a steady hand, a kind word, or at least someone brave enough to stop and listen.', choices: [
                { id: 'help', label: 'Step in and help', effectKey: 'help' },
                { id: 'refuse', label: 'Keep your distance', effectKey: 'refuse' },
                { id: 'exploit', label: 'Turn the mess to your advantage', effectKey: 'exploit' }
            ] };
        }
        if (def.template === 'trade_offer') {
            return { title: def.title, icon: def.icon, text: lead + ' {npcName} murmurs that a bundle of {resourceName} can be had for {costGold} gold, but only if you answer before the moment cools.', choices: [
                { id: 'buy', label: 'Buy the lot for {costGold}g', effectKey: 'buy', requires: { gold: 'costGold' } },
                { id: 'pass', label: 'Let the offer pass', effectKey: 'pass' },
                { id: 'report', label: 'Warn the market watch', effectKey: 'report' }
            ] };
        }
        if (def.template === 'social_scene') {
            return { title: def.title, icon: def.icon, text: lead + ' {npcName} holds your gaze a heartbeat too long, and the world around you seems suddenly eager for scandal.', choices: [
                { id: 'encourage', label: 'Lean into the moment', effectKey: 'encourage' },
                { id: 'polite', label: 'Answer with grace and distance', effectKey: 'polite' },
                { id: 'cruel', label: 'Cut the moment dead', effectKey: 'cruel' }
            ] };
        }
        if (def.template === 'crime_scene') {
            return { title: def.title, icon: def.icon, text: lead + ' {npcName} offers a grin better suited to a knife than a smile and suggests there is profit in acting quickly.', choices: [
                { id: 'join', label: 'Take the risky profit', effectKey: 'join' },
                { id: 'refuse', label: 'Refuse the scheme', effectKey: 'refuse' },
                { id: 'report_crime', label: 'Tip off the authorities', effectKey: 'report_crime' }
            ] };
        }
        if (def.template === 'skill_test') {
            return { title: def.title, icon: def.icon, text: lead + ' Someone in {townName} lays down a test of nerve, wit, or craftsmanship and waits to see whether {playerName} will answer.', choices: [
                { id: 'attempt', label: 'Attempt the challenge', effectKey: 'attempt' },
                { id: 'bet_safe', label: 'Play it safe and learn what you can', effectKey: 'bet_safe' },
                { id: 'pass', label: 'Decline without shame', effectKey: 'pass' }
            ] };
        }
        if (def.template === 'delayed_notice') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' {npcName} promises that if you involve yourself now, news will find you again in {waitDays} days.', choices: [
                { id: 'accept', label: 'Get involved', effectKey: 'accept', nextStepIndex: 1 },
                { id: 'decline', label: 'Decline and move on', effectKey: 'decline' },
                { id: 'exploit', label: 'Twist the matter for quick coin', effectKey: 'exploit' }
            ] };
            return { title: def.title, icon: def.icon, text: 'As promised, the echo of ' + def.title.toLowerCase() + ' returns to you. {npcName} arrives with the result and a choice of what sort of name you want made from it.', choices: [
                { id: 'claim', label: 'Take the reward openly', effectKey: 'claim' },
                { id: 'spread', label: 'Turn the tale toward goodwill', effectKey: 'spread' },
                { id: 'withdraw', label: 'Quietly let the matter end', effectKey: 'withdraw' }
            ] };
        }
        if (def.template === 'trade_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' A speculative deal in {resourceName} is whispered your way. Back it now, and in {waitDays} days the market will judge you.', choices: [
                { id: 'commit', label: 'Commit {costGold}g to the venture', effectKey: 'commit', nextStepIndex: 1, requires: { gold: 'costGold' } },
                { id: 'haggle', label: 'Commit less and haggle hard', effectKey: 'haggle', nextStepIndex: 1, requires: { gold: 'costGold' } },
                { id: 'pass', label: 'Decline the risk', effectKey: 'pass' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The venture matures. The {resourceName} arrives, the ledgers shake, and {npcName} asks how you want to end the bargain.', choices: [
                { id: 'unload', label: 'Unload the goods for profit', effectKey: 'unload' },
                { id: 'flip', label: 'Make a ruthless quick sale', effectKey: 'flip' },
                { id: 'donate', label: 'Divert part of it to goodwill', effectKey: 'donate' }
            ] };
        }
        if (def.template === 'romance_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' {npcName} leaves no doubt that this is not small talk. A meeting in {waitDays} days hangs on whatever you say next.', choices: [
                { id: 'encourage', label: 'Encourage the advance', effectKey: 'encourage', nextStepIndex: 1 },
                { id: 'polite', label: 'Refuse gently', effectKey: 'polite' },
                { id: 'cruel', label: 'Refuse with ice', effectKey: 'cruel' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The promised rendezvous arrives under lantern light. {npcName} is waiting with a hopeful smile and just enough courage to be dangerous.', choices: [
                { id: 'attend', label: 'Attend the rendezvous', effectKey: 'attend' },
                { id: 'bring_gift', label: 'Attend with a gift of {costGold}g', effectKey: 'bring_gift', requires: { gold: 'costGold' } },
                { id: 'stay_away', label: 'Do not appear', effectKey: 'stay_away' }
            ] };
        }
        if (def.template === 'investigation_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' A thread dangles before you. Tug it, and in {waitDays} days you may learn something worth coin, blood, or both.', choices: [
                { id: 'investigate', label: 'Investigate quietly', effectKey: 'investigate', nextStepIndex: 1 },
                { id: 'sell_secret', label: 'Sell the lead immediately', effectKey: 'sell_secret' },
                { id: 'ignore', label: 'Leave the darkness sleeping', effectKey: 'ignore' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The clue matures into something sharp. {npcName} now stands exposed in a way that could be profitable, righteous, or very foolish.', choices: [
                { id: 'confront', label: 'Confront them directly', effectKey: 'confront' },
                { id: 'report_crime', label: 'Hand the matter to the authorities', effectKey: 'report_crime' },
                { id: 'pocket', label: 'Pocket what you can and vanish', effectKey: 'pocket' }
            ] };
        }
        if (def.template === 'war_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' {npcName} begs for aid that may matter by the frontier. Your answer will come back to you in {waitDays} days, one way or another.', choices: [
                { id: 'assist', label: 'Assist the war effort', effectKey: 'assist', nextStepIndex: 1 },
                { id: 'profit', label: 'Help, but think of profit first', effectKey: 'profit', nextStepIndex: 1 },
                { id: 'decline', label: 'Keep clear of the army', effectKey: 'decline' }
            ] };
            return { title: def.title, icon: def.icon, text: 'A tired messenger returns from the front. Whatever you set in motion has borne fruit, and now the realm remembers your name.', choices: [
                { id: 'accept_reward', label: 'Accept the reward', effectKey: 'accept_reward' },
                { id: 'donate_reward', label: 'Donate the reward to the cause', effectKey: 'donate_reward' },
                { id: 'ask_for_favor', label: 'Ask instead for favor and goodwill', effectKey: 'ask_for_favor' }
            ] };
        }
        if (def.template === 'political_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' {npcName} speaks in careful half-sentences and asks whether you will back a move that could echo through {kingdomName} in {waitDays} days.', choices: [
                { id: 'support', label: 'Support the maneuver', effectKey: 'support', nextStepIndex: 1 },
                { id: 'leak', label: 'Sell the whisper to someone else', effectKey: 'leak' },
                { id: 'decline', label: 'Stay above the scheme', effectKey: 'decline' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The court has shifted. Doors open, others slam, and {npcName} returns to ask what kind of victor you intend to be.', choices: [
                { id: 'press_advantage', label: 'Press your advantage', effectKey: 'press_advantage' },
                { id: 'take_gift', label: 'Take a discreet gift and be content', effectKey: 'take_gift' },
                { id: 'step_back', label: 'Step back before envy wakes', effectKey: 'step_back' }
            ] };
        }
        if (def.template === 'rank_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' Someone lower on the ladder kneels, bows, or at least performs the local equivalent, and asks what sort of ruler or notable you mean to be.', choices: [
                { id: 'hear_them_out', label: 'Hear them out seriously', effectKey: 'hear_them_out', nextStepIndex: 1 },
                { id: 'exact_toll', label: 'Demand tribute for your attention', effectKey: 'exact_toll', nextStepIndex: 1 },
                { id: 'brush_aside', label: 'Brush them aside', effectKey: 'brush_aside' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The matter ripens into a public answer. Eyes in {townName} watch to learn whether your station means mercy, greed, or simple exhaustion.', choices: [
                { id: 'grant_mercy', label: 'Grant mercy', effectKey: 'grant_mercy' },
                { id: 'take_tribute', label: 'Take tribute and settle it', effectKey: 'take_tribute' },
                { id: 'close_case', label: 'Close the matter quietly', effectKey: 'close_case' }
            ] };
        }
        if (def.template === 'mystic_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' Whether trick, omen, or the boredom of the gods, something in the scene asks whether {playerName} still knows how to listen.', choices: [
                { id: 'heed', label: 'Heed the omen', effectKey: 'heed', nextStepIndex: 1 },
                { id: 'listen', label: 'Listen, but stay cautious', effectKey: 'listen', nextStepIndex: 1 },
                { id: 'mock', label: 'Laugh it off', effectKey: 'mock' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The omen returns after {waitDays} days, more insistent and far less willing to be ignored. Even daylight seems to lean in.', choices: [
                { id: 'follow', label: 'Follow where it leads', effectKey: 'follow' },
                { id: 'ward', label: 'Ward yourself and turn away', effectKey: 'ward', requires: { gold: 'costGold' } },
                { id: 'refuse', label: 'Refuse the call', effectKey: 'refuse' }
            ] };
        }
        if (def.template === 'skill_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' A challenge is set and judges are promised in {waitDays} days. You can enter now or spare yourself the embarrassment.', choices: [
                { id: 'accept', label: 'Enter the challenge', effectKey: 'accept', nextStepIndex: 1 },
                { id: 'bet_safe', label: 'Observe and prepare instead', effectKey: 'bet_safe' },
                { id: 'decline', label: 'Decline the challenge', effectKey: 'decline' }
            ] };
            return { title: def.title, icon: def.icon, text: 'Judgment day arrives. The crowd wants a winner, the masters want form, and your rivals want a story at your expense.', choices: [
                { id: 'collect_prize', label: 'Collect your prize proudly', effectKey: 'collect_prize' },
                { id: 'share_credit', label: 'Share credit with the others', effectKey: 'share_credit' },
                { id: 'walk', label: 'Walk away before vanity bites', effectKey: 'walk' }
            ] };
        }
        if (def.template === 'context_chain') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' The town itself seems to beg for intervention. Act now, and in {waitDays} days the streets will tell you whether it mattered.', choices: [
                { id: 'intervene', label: 'Intervene directly', effectKey: 'intervene', nextStepIndex: 1 },
                { id: 'profit', label: 'Profit from the chaos', effectKey: 'profit', nextStepIndex: 1 },
                { id: 'observe', label: 'Observe and keep your hands clean', effectKey: 'observe' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The situation breaks one way or the other. {townName} remembers what you did, even if it does not remember kindly.', choices: [
                { id: 'take_reward', label: 'Take what reward is offered', effectKey: 'take_reward' },
                { id: 'reinvest', label: 'Reinvest the gain into the town', effectKey: 'reinvest', requires: { gold: 'costGold' } },
                { id: 'leave', label: 'Leave before gratitude turns to requests', effectKey: 'leave' }
            ] };
        }
        if (def.template === 'long_omen') {
            if (step === 0) return { title: def.title, icon: def.icon, text: lead + ' The first sign is subtle, almost polite. The second will not be, if you invite it.', choices: [
                { id: 'heed', label: 'Step toward the omen', effectKey: 'heed', nextStepIndex: 1 },
                { id: 'mock', label: 'Spit in the eye of omens', effectKey: 'mock' }
            ] };
            if (step === 1) return { title: def.title, icon: def.icon, text: 'After {waitDays} days the dream ripens into a pattern. Faces repeat. Footsteps echo where nobody walks.', choices: [
                { id: 'listen', label: 'Listen more carefully', effectKey: 'listen', nextStepIndex: 2 },
                { id: 'refuse', label: 'Refuse the pattern', effectKey: 'refuse' }
            ] };
            if (step === 2) return { title: def.title, icon: def.icon, text: 'After another {waitDays2} days the sign manifests in waking life. A choice must now become a deed.', choices: [
                { id: 'follow', label: 'Follow the sign to its source', effectKey: 'follow', nextStepIndex: 3 },
                { id: 'ward', label: 'Purchase wards and holy quiet', effectKey: 'ward', requires: { gold: 'costGold' } }
            ] };
            if (step === 3) return { title: def.title, icon: def.icon, text: 'At the source of the omen you find not certainty, but leverage: fear, wonder, and a prize that may not love its owner.', choices: [
                { id: 'claim', label: 'Claim what the omen offers', effectKey: 'claim', nextStepIndex: 4 },
                { id: 'spread', label: 'Turn the omen into a public tale', effectKey: 'spread', nextStepIndex: 4 },
                { id: 'withdraw', label: 'Withdraw while you still can', effectKey: 'withdraw' }
            ] };
            return { title: def.title, icon: def.icon, text: 'The final echo fades. Only consequence remains, and whatever name the town chooses to give you after this.', choices: [
                { id: 'take_reward', label: 'Take the last reward', effectKey: 'take_reward' },
                { id: 'mark_secret', label: 'Keep the thing secret', effectKey: 'mark_secret' },
                { id: 'leave', label: 'Leave the mystery buried', effectKey: 'leave' }
            ] };
        }
        return { title: def.title, icon: def.icon, text: lead, choices: [{ id: 'leave', label: 'Move on', effectKey: 'leave' }] };
    }

    function _requireReason(choice, params) {
        if (!choice || !choice.requires) return null;
        if (choice.requires.gold) {
            var need = params[choice.requires.gold] || choice.requires.gold;
            if ((player.gold || 0) < need) return 'Need ' + need + ' gold.';
        }
        return null;
    }
    function _applyInventory(resourceId, delta) {
        if (!resourceId || !delta) return;
        player.inventory[resourceId] = (player.inventory[resourceId] || 0) + delta;
        if (player.inventory[resourceId] <= 0) delete player.inventory[resourceId];
    }
    function _rep(kid, delta) {
        if (delta && kid) try { Player.modifyReputation(kid, delta); } catch (e) {}
    }
    function _rel(pid, delta) {
        if (delta && pid) try { Player.modifyRelationship(pid, delta); } catch (e) {}
    }
    function _gold(delta) {
        if (delta) try { Player.modifyGold(delta, 'unsolicited_event'); } catch (e) {}
    }
    function _energy(delta) {
        if (delta) try { Player.modifyEnergy(delta); } catch (e) {}
    }
    function _hazard(def, ctx) {
        var extra = _derivedExtra(def);
        if (extra.hazard && ctx && ctx.rng && ctx.rng.chance(0.15)) {
            try { Player.inflictRandomInjury('unsolicited_event:' + def.id); } catch (e) {}
        }
    }
    var EVENT_DEFS = [
        {
            id: "found_coin_purse",
            title: "Found Coin Purse",
            icon: "💰",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "stray_dog",
            title: "Stray Dog",
            icon: "🐕",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "street_performer",
            title: "Street Performer",
            icon: "🎭",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "dropped_cargo",
            title: "Dropped Cargo",
            icon: "📦",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "overheard_gossip",
            title: "Overheard Gossip",
            icon: "🫖",
            category: "common",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "child_lost",
            title: "Child Lost",
            icon: "🧒",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "drunk_stranger",
            title: "Drunk Stranger",
            icon: "🍺",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "rain_shelter",
            title: "Rain Shelter",
            icon: "🌧️",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "merchant_argument",
            title: "Merchant Argument",
            icon: "✨",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "beggar_plea",
            title: "Beggar Plea",
            icon: "��",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "broken_wheel",
            title: "Broken Wheel",
            icon: "🛞",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "town_crier",
            title: "Town Crier",
            icon: "📯",
            category: "common",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "old_woman_herbs",
            title: "Old Woman Herbs",
            icon: "🌿",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "festival_game",
            title: "Festival Game",
            icon: "🎪",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "mysterious_letter",
            title: "Mysterious Letter",
            icon: "✉️",
            category: "common",
            rarity: "rare",
            weight: 6,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "food_vendor",
            title: "Food Vendor",
            icon: "🛒",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "bard_tale",
            title: "Bard Tale",
            icon: "🎶",
            category: "common",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "cat_in_tree",
            title: "Cat In Tree",
            icon: "🐈",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "neighbor_dispute",
            title: "Neighbor Dispute",
            icon: "✨",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "sunset_view",
            title: "Sunset View",
            icon: "🌇",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "market_bargain",
            title: "Market Bargain",
            icon: "✨",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "traveling_monk",
            title: "Traveling Monk",
            icon: "🙏",
            category: "common",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "dropped_wallet",
            title: "Dropped Wallet",
            icon: "👛",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "strange_smell",
            title: "Strange Smell",
            icon: "👃",
            category: "common",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "public_speech",
            title: "Public Speech",
            icon: "🗣️",
            category: "common",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "butterfly_garden",
            title: "Butterfly Garden",
            icon: "🦋",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "old_map",
            title: "Old Map",
            icon: "🗺️",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "well_wishing",
            title: "Well Wishing",
            icon: "✨",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "friendly_guard",
            title: "Friendly Guard",
            icon: "🛡️",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "runaway_horse",
            title: "Runaway Horse",
            icon: "🐎",
            category: "common",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "shady_deal",
            title: "Shady Deal",
            icon: "🤝",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "price_crash_tip",
            title: "Price Crash Tip",
            icon: "📉",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "merchant_bankruptcy",
            title: "Merchant Bankruptcy",
            icon: "📉",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "counterfeit_coins",
            title: "Counterfeit Coins",
            icon: "💰",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "tax_collector_bribe",
            title: "Tax Collector Bribe",
            icon: "💰",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "caravan_opportunity",
            title: "Caravan Opportunity",
            icon: "🐪",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "trade_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "warehouse_fire",
            title: "Warehouse Fire",
            icon: "🔥",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "trade_embargo_rumor",
            title: "Trade Embargo Rumor",
            icon: "🚫",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "bulk_buyer",
            title: "Bulk Buyer",
            icon: "💼",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "trade_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "investment_scam",
            title: "Investment Scam",
            icon: "🎲",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "trade_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "supply_glut",
            title: "Supply Glut",
            icon: "📦",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "trade_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "rare_goods_vendor",
            title: "Rare Goods Vendor",
            icon: "🛒",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "market_theft",
            title: "Market Theft",
            icon: "📦",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "debt_collector",
            title: "Debt Collector",
            icon: "📦",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "trade_secret",
            title: "Trade Secret",
            icon: "📦",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "customs_inspection",
            title: "Customs Inspection",
            icon: "📦",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "smuggler_offer",
            title: "Smuggler Offer",
            icon: "📦",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "guild_dues",
            title: "Guild Dues",
            icon: "📦",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "price_war_tip",
            title: "Price War Tip",
            icon: "📉",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "cargo_insurance",
            title: "Cargo Insurance",
            icon: "📦",
            category: "trade",
            rarity: "uncommon",
            weight: 8,
            template: "trade_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "apprentice_offer",
            title: "Apprentice Offer",
            icon: "📦",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "foreign_currency",
            title: "Foreign Currency",
            icon: "🪙",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "raw_material_find",
            title: "Raw Material Find",
            icon: "⛏️",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "rigged_scale",
            title: "Rigged Scale",
            icon: "⚖️",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "trade_offer",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "dock_worker_strike",
            title: "Dock Worker Strike",
            icon: "✊",
            category: "trade",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "love_confession",
            title: "Love Confession",
            icon: "💌",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "jealous_rival",
            title: "Jealous Rival",
            icon: "💬",
            category: "social",
            rarity: "common",
            weight: 10,
            template: "social_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "old_friend",
            title: "Old Friend",
            icon: "💬",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "wedding_invitation",
            title: "Wedding Invitation",
            icon: "💒",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "flirtatious_stranger",
            title: "Flirtatious Stranger",
            icon: "😘",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "anonymous_love_letter",
            title: "Anonymous Love Letter",
            icon: "✉️",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "matchmaker",
            title: "Matchmaker",
            icon: "💐",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "heartbreak_witness",
            title: "Heartbreak Witness",
            icon: "💔",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "dance_invitation",
            title: "Dance Invitation",
            icon: "💃",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "gift_from_admirer",
            title: "Gift From Admirer",
            icon: "🎁",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "family_reunion",
            title: "Family Reunion",
            icon: "🫂",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "tavern_companion",
            title: "Tavern Companion",
            icon: "💬",
            category: "social",
            rarity: "common",
            weight: 10,
            template: "social_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "love_triangle",
            title: "Love Triangle",
            icon: "💌",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "serenade",
            title: "Serenade",
            icon: "🎵",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "poetry_reading",
            title: "Poetry Reading",
            icon: "🪶",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "childhood_sweetheart",
            title: "Childhood Sweetheart",
            icon: "🧒",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "marriage_proposal_stranger",
            title: "Marriage Proposal Stranger",
            icon: "💍",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "gossip_about_you",
            title: "Gossip About You",
            icon: "🫖",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "pet_name",
            title: "Pet Name",
            icon: "📝",
            category: "social",
            rarity: "uncommon",
            weight: 8,
            template: "romance_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "couple_fight_mediator",
            title: "Couple Fight Mediator",
            icon: "🥊",
            category: "social",
            rarity: "common",
            weight: 10,
            template: "social_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "pickpocket_attempt",
            title: "Pickpocket Attempt",
            icon: "🪙",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "mugging",
            title: "Mugging",
            icon: "🥊",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "bar_fight",
            title: "Bar Fight",
            icon: "🥊",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "arson_witness",
            title: "Arson Witness",
            icon: "🔥",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "smuggler_approach",
            title: "Smuggler Approach",
            icon: "📦",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "prison_break_witness",
            title: "Prison Break Witness",
            icon: "🔓",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "con_artist",
            title: "Con Artist",
            icon: "🎭",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "corrupt_guard",
            title: "Corrupt Guard",
            icon: "🛡️",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "threatening_note",
            title: "Threatening Note",
            icon: "✉️",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "hired_thug",
            title: "Hired Thug",
            icon: "🗡️",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "witness_crime",
            title: "Witness Crime",
            icon: "🕵️",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "fence_offer",
            title: "Fence Offer",
            icon: "🗝️",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "gang_recruitment",
            title: "Gang Recruitment",
            icon: "🩸",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "poison_in_drink",
            title: "Poison In Drink",
            icon: "☠️",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "night_ambush",
            title: "Night Ambush",
            icon: "🌃",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "bounty_posted",
            title: "Bounty Posted",
            icon: "📜",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "protection_racket",
            title: "Protection Racket",
            icon: "💸",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "stolen_goods_found",
            title: "Stolen Goods Found",
            icon: "🗡️",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "false_accusation",
            title: "False Accusation",
            icon: "🗡️",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "underground_gambling",
            title: "Underground Gambling",
            icon: "🎲",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "loan_shark",
            title: "Loan Shark",
            icon: "💸",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "mysterious_package",
            title: "Mysterious Package",
            icon: "📦",
            category: "crime",
            rarity: "rare",
            weight: 6,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "duel_challenge",
            title: "Duel Challenge",
            icon: "⚔️",
            category: "crime",
            rarity: "common",
            weight: 10,
            template: "crime_scene",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "safe_cracker",
            title: "Safe Cracker",
            icon: "🔐",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "escape_artist",
            title: "Escape Artist",
            icon: "🎭",
            category: "crime",
            rarity: "uncommon",
            weight: 8,
            template: "investigation_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "war_deserter",
            title: "War Deserter",
            icon: "🏃",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "war_refugee",
            title: "War Refugee",
            icon: "🏚️",
            category: "war",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "military_recruiter",
            title: "Military Recruiter",
            icon: "🪖",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "spy_approach",
            title: "Spy Approach",
            icon: "🕵️",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "war_profiteer",
            title: "War Profiteer",
            icon: "💼",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "wounded_soldier",
            title: "Wounded Soldier",
            icon: "🩹",
            category: "war",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "enemy_prisoner",
            title: "Enemy Prisoner",
            icon: "🔓",
            category: "war",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "propaganda",
            title: "Propaganda",
            icon: "📣",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "war_tax",
            title: "War Tax",
            icon: "💰",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "supply_line_cut",
            title: "Supply Line Cut",
            icon: "⚔️",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "battle_news",
            title: "Battle News",
            icon: "⚔️",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "conscription_dodge",
            title: "Conscription Dodge",
            icon: "⚔️",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "war_memorial",
            title: "War Memorial",
            icon: "🪦",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "siege_preparation",
            title: "Siege Preparation",
            icon: "🏰",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "war_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "peace_rumor",
            title: "Peace Rumor",
            icon: "🕊️",
            category: "war",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "petition_gatherer",
            title: "Petition Gatherer",
            icon: "📜",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "noble_scandal",
            title: "Noble Scandal",
            icon: "🏰",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "royal_decree",
            title: "Royal Decree",
            icon: "👑",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "tax_protest",
            title: "Tax Protest",
            icon: "💰",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "corruption_witness",
            title: "Corruption Witness",
            icon: "📜",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "election_campaign",
            title: "Election Campaign",
            icon: "🗳️",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "land_dispute",
            title: "Land Dispute",
            icon: "📜",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "censorship",
            title: "Censorship",
            icon: "🚫",
            category: "political",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "treason_whisper",
            title: "Treason Whisper",
            icon: "🗡️",
            category: "political",
            rarity: "epic",
            weight: 4,
            template: "long_omen",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "diplomatic_visit",
            title: "Diplomatic Visit",
            icon: "🤝",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "court_summons",
            title: "Court Summons",
            icon: "⚖️",
            category: "political",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "succession_crisis",
            title: "Succession Crisis",
            icon: "👑",
            category: "political",
            rarity: "epic",
            weight: 4,
            template: "long_omen",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "law_debate",
            title: "Law Debate",
            icon: "📚",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "political_pamphlet",
            title: "Political Pamphlet",
            icon: "📄",
            category: "political",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "noble_feud",
            title: "Noble Feud",
            icon: "🏰",
            category: "political",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "fortune_teller",
            title: "Fortune Teller",
            icon: "🔮",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "cursed_item",
            title: "Cursed Item",
            icon: "🪬",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "wandering_monk",
            title: "Wandering Monk",
            icon: "🙏",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "dream_vision",
            title: "Dream Vision",
            icon: "💤",
            category: "supernatural",
            rarity: "epic",
            weight: 4,
            template: "long_omen",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "ghost_sighting",
            title: "Ghost Sighting",
            icon: "👻",
            category: "supernatural",
            rarity: "epic",
            weight: 4,
            template: "long_omen",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "herbal_healer",
            title: "Herbal Healer",
            icon: "🔮",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "eclipse_omen",
            title: "Eclipse Omen",
            icon: "🌘",
            category: "supernatural",
            rarity: "epic",
            weight: 4,
            template: "long_omen",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "talking_beggar",
            title: "Talking Beggar",
            icon: "��",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "alchemist_experiment",
            title: "Alchemist Experiment",
            icon: "⚗️",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "ancient_ruin",
            title: "Ancient Ruin",
            icon: "🏛️",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "prophetic_child",
            title: "Prophetic Child",
            icon: "🧒",
            category: "supernatural",
            rarity: "epic",
            weight: 4,
            template: "long_omen",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "midnight_visitor",
            title: "Midnight Visitor",
            icon: "🚪",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "strange_animal",
            title: "Strange Animal",
            icon: "🦊",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "lucky_charm",
            title: "Lucky Charm",
            icon: "🍀",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "old_legend",
            title: "Old Legend",
            icon: "📖",
            category: "supernatural",
            rarity: "rare",
            weight: 6,
            template: "mystic_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "medical_emergency",
            title: "Medical Emergency",
            icon: "🩺",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "forgery_request",
            title: "Forgery Request",
            icon: "🖋️",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "alchemy_accident",
            title: "Alchemy Accident",
            icon: "⚗️",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "combat_training",
            title: "Combat Training",
            icon: "🌧️",
            category: "skill",
            rarity: "common",
            weight: 10,
            template: "skill_test",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "lockpick_challenge",
            title: "Lockpick Challenge",
            icon: "🗝️",
            category: "skill",
            rarity: "common",
            weight: 10,
            template: "skill_test",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "navigation_test",
            title: "Navigation Test",
            icon: "🧭",
            category: "skill",
            rarity: "common",
            weight: 10,
            template: "skill_test",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "persuasion_test",
            title: "Persuasion Test",
            icon: "🗣️",
            category: "skill",
            rarity: "common",
            weight: 10,
            template: "skill_test",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "appraisal_moment",
            title: "Appraisal Moment",
            icon: "💎",
            category: "skill",
            rarity: "common",
            weight: 10,
            template: "skill_test",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "cooking_contest",
            title: "Cooking Contest",
            icon: "🍲",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "animal_taming",
            title: "Animal Taming",
            icon: "🦊",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "teaching_request",
            title: "Teaching Request",
            icon: "📚",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "architecture_opinion",
            title: "Architecture Opinion",
            icon: "📐",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "healing_herbs",
            title: "Healing Herbs",
            icon: "🌿",
            category: "skill",
            rarity: "common",
            weight: 10,
            template: "skill_test",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "instrument_found",
            title: "Instrument Found",
            icon: "🎻",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "sword_in_stone",
            title: "Sword In Stone",
            icon: "🪨",
            category: "skill",
            rarity: "uncommon",
            weight: 8,
            template: "skill_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "peasant_plea",
            title: "Peasant Plea",
            icon: "🧑‍🌾",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "merchant_rivalry",
            title: "Merchant Rivalry",
            icon: "💼",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "noble_duel",
            title: "Noble Duel",
            icon: "⚔️",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "court_gossip",
            title: "Court Gossip",
            icon: "🫖",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "tax_exemption_offer",
            title: "Tax Exemption Offer",
            icon: "💰",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "land_grant",
            title: "Land Grant",
            icon: "📜",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "royal_audience",
            title: "Royal Audience",
            icon: "👑",
            category: "rank",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "assassination_plot",
            title: "Assassination Plot",
            icon: "🗡️",
            category: "rank",
            rarity: "rare",
            weight: 6,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "noble_alliance",
            title: "Noble Alliance",
            icon: "🏰",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "commoner_uprising",
            title: "Commoner Uprising",
            icon: "🔥",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "inheritance_dispute",
            title: "Inheritance Dispute",
            icon: "👑",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "guild_leadership",
            title: "Guild Leadership",
            icon: "🏅",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "diplomatic_mission",
            title: "Diplomatic Mission",
            icon: "🤝",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "noble_banquet",
            title: "Noble Banquet",
            icon: "🏰",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "crown_counsel",
            title: "Crown Counsel",
            icon: "👂",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "political_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "quarantine_escape",
            title: "Quarantine Escape",
            icon: "☣️",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "context_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "plague_healer",
            title: "Plague Healer",
            icon: "☣️",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "famine_hoarding",
            title: "Famine Hoarding",
            icon: "🥣",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "context_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "famine_soup_kitchen",
            title: "Famine Soup Kitchen",
            icon: "🥣",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "boom_town_investor",
            title: "Boom Town Investor",
            icon: "🏗️",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "boom_town_complaint",
            title: "Boom Town Complaint",
            icon: "🏗️",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "crime_wave_vigilante",
            title: "Crime Wave Vigilante",
            icon: "🕵️",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "context_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "crime_wave_victim",
            title: "Crime Wave Victim",
            icon: "🕵️",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "price_gouging",
            title: "Price Gouging",
            icon: "📉",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "context_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "depression_merchant",
            title: "Depression Merchant",
            icon: "📉",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "context_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "frontline_scout",
            title: "Frontline Scout",
            icon: "⚔️",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "context_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "frontline_casualty",
            title: "Frontline Casualty",
            icon: "⚔️",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "war_orphan",
            title: "War Orphan",
            icon: "🧒",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "security_checkpoint",
            title: "Security Checkpoint",
            icon: "🛡️",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "wealthy_tourist",
            title: "Wealthy Tourist",
            icon: "💎",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "windfall",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "drought_water",
            title: "Drought Water",
            icon: "🪣",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "bandit_warning",
            title: "Bandit Warning",
            icon: "⚠️",
            category: "context",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "refugee_camp",
            title: "Refugee Camp",
            icon: "🏚️",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "morale_boost",
            title: "Morale Boost",
            icon: "📣",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "harvest_festival",
            title: "Harvest Festival",
            icon: "🎪",
            category: "context",
            rarity: "common",
            weight: 10,
            template: "aid",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "fame_recognition",
            title: "Fame Recognition",
            icon: "🌟",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "old_rival_returns",
            title: "Old Rival Returns",
            icon: "👑",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "delayed_notice",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "legacy_challenge",
            title: "Legacy Challenge",
            icon: "👑",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "mentor_student",
            title: "Mentor Student",
            icon: "📚",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "wealth_display",
            title: "Wealth Display",
            icon: "💎",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "legend_in_making",
            title: "Legend In Making",
            icon: "📖",
            category: "rank",
            rarity: "rare",
            weight: 6,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "humble_origins",
            title: "Humble Origins",
            icon: "🌱",
            category: "rank",
            rarity: "uncommon",
            weight: 8,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "dynasty_founder",
            title: "Dynasty Founder",
            icon: "👑",
            category: "rank",
            rarity: "rare",
            weight: 6,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "master_trader",
            title: "Master Trader",
            icon: "👑",
            category: "rank",
            rarity: "rare",
            weight: 6,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        },
        {
            id: "notorious_reputation",
            title: "Notorious Reputation",
            icon: "☠️",
            category: "rank",
            rarity: "rare",
            weight: 6,
            template: "rank_chain",
            condition: function(ctx) { return _passesCondition(ctx, this); },
            generate: function(ctx) {
                var extra = _derivedExtra(this);
                var rng = ctx && ctx.rng;
                var base;
                var npc;
                var resourceId;
                var params;
                if (!ctx || !rng || !player || !ctx.town) return null;
                base = _rarityBase(this.rarity);
                npc = _pickNpc(ctx, extra.npcRole);
                resourceId = _chooseFromGroup(this, ctx);
                params = {
                    townName: ctx.townName,
                    kingdomName: ctx.kingdomName,
                    playerName: ctx.playerName,
                    npcName: npc ? (npc.firstName || 'a stranger') : 'a stranger',
                    goldAmount: _roll(rng, base.gold),
                    repAmount: _roll(rng, base.rep),
                    relAmount: _roll(rng, base.rel),
                    energyAmount: _roll(rng, base.energy),
                    itemQty: _roll(rng, base.qty),
                    waitDays: _roll(rng, base.wait),
                    waitDays2: _roll(rng, [1, Math.max(2, base.wait[1])]),
                    resourceId: resourceId,
                    resourceName: RESOURCE_NAMES[resourceId] || resourceId,
                    costGold: Math.max(6, Math.floor(_roll(rng, base.gold) * 0.5)),
                    rewardGold: Math.max(10, Math.floor(_roll(rng, base.gold) * 1.2))
                };
                return {
                    id: 'ue_' + (player._nextUnsolicitedEventId++),
                    defId: this.id,
                    stepIndex: 0,
                    params: params,
                    generatedDay: ctx.day,
                    dueDay: ctx.day,
                    status: 'pending',
                    choiceHistory: [],
                    townId: ctx.town ? ctx.town.id : null,
                    kingdomId: ctx.kingdomId || null,
                    npcId: npc ? npc.id : null
                };
            },
            applyEffect: function(effectKey, params, ctx) {
                var def = this;
                var kid = (params && params.kingdomId) || (ctx && ctx.kingdomId) || _currentKingdomId();
                var pid = (params && params.npcId) || (ctx && ctx.pendingNpcId) || (player._pendingUnsolicitedEvent && player._pendingUnsolicitedEvent.npcId) || null;
                switch (effectKey) {
                    case 'pocket': _gold(params.goldAmount); if (def.category === 'crime') _rep(kid, -params.repAmount); break;
                    case 'share': _gold(Math.max(1, Math.floor(params.goldAmount / 3))); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'leave': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'help': _energy(-params.energyAmount); _rep(kid, params.repAmount); _rel(pid, params.relAmount); break;
                    case 'refuse': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'exploit': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'buy': _gold(-params.costGold); _applyInventory(params.resourceId, params.itemQty); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'pass': _energy(Math.max(1, Math.floor(params.energyAmount / 3))); break;
                    case 'report': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'encourage': _rel(pid, params.relAmount); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'polite': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'cruel': _rel(pid, -params.relAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'join': _gold(params.goldAmount); _rep(kid, -params.repAmount); _hazard(def, ctx); break;
                    case 'report_crime': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'accept': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'decline': break;
                    case 'claim': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'spread': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'withdraw': _energy(params.energyAmount); break;
                    case 'commit': _gold(-params.costGold); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'haggle': _gold(-Math.max(1, Math.floor(params.costGold * 0.7))); _rel(pid, 1); break;
                    case 'unload': _gold(params.rewardGold); _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2))); break;
                    case 'flip': _gold(params.rewardGold + Math.max(2, Math.floor(params.goldAmount / 2))); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'donate': _rep(kid, params.repAmount + 1); break;
                    case 'attend': _rel(pid, params.relAmount + 1); _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'bring_gift': _gold(-params.costGold); _rel(pid, params.relAmount + 2); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'stay_away': _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'investigate': _energy(-params.energyAmount); break;
                    case 'sell_secret': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'ignore': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'confront': _rep(kid, params.repAmount); _rel(pid, -Math.max(1, Math.floor(params.relAmount / 2))); _hazard(def, ctx); break;
                    case 'assist': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'profit': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'accept_reward': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'donate_reward': _rep(kid, params.repAmount + 2); break;
                    case 'ask_for_favor': _rel(pid, params.relAmount); _rep(kid, params.repAmount + 1); break;
                    case 'support': _rep(kid, params.repAmount); _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); break;
                    case 'leak': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'press_advantage': _gold(params.rewardGold); _rep(kid, params.repAmount + 1); break;
                    case 'take_gift': _gold(params.goldAmount); _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'step_back': _energy(params.energyAmount); break;
                    case 'hear_them_out': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'exact_toll': _gold(params.goldAmount); _rep(kid, -params.repAmount); break;
                    case 'brush_aside': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'grant_mercy': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'take_tribute': _gold(params.rewardGold); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'close_case': _energy(params.energyAmount); break;
                    case 'heed': _energy(-Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'listen': _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'mock': _rep(kid, -Math.max(1, Math.floor(params.repAmount / 2))); break;
                    case 'follow': _gold(Math.max(5, Math.floor(params.goldAmount / 2))); _rep(kid, params.repAmount); _hazard(def, ctx); break;
                    case 'ward': _gold(-params.costGold); _energy(params.energyAmount); break;
                    case 'attempt': _energy(-params.energyAmount); _gold(Math.max(4, Math.floor(params.goldAmount / 2))); _hazard(def, ctx); break;
                    case 'bet_safe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); _rep(kid, 1); break;
                    case 'collect_prize': _gold(params.rewardGold); _rep(kid, params.repAmount); break;
                    case 'share_credit': _rep(kid, params.repAmount + 1); _rel(pid, params.relAmount); break;
                    case 'walk': _energy(params.energyAmount); break;
                    case 'intervene': _energy(-params.energyAmount); _rep(kid, params.repAmount); break;
                    case 'observe': _energy(Math.max(1, Math.floor(params.energyAmount / 2))); break;
                    case 'take_reward': _gold(params.rewardGold); break;
                    case 'reinvest': _gold(-params.costGold); _rep(kid, params.repAmount + 2); break;
                    case 'mark_secret': _rel(pid, Math.max(1, Math.floor(params.relAmount / 2))); _rep(kid, Math.max(1, Math.floor(params.repAmount / 2))); break;
                }
                if ((def.category === 'trade' || def.category === 'context' || def.category === 'skill') && (effectKey === 'take_reward' || effectKey === 'collect_prize')) {
                    _applyInventory(params.resourceId, Math.max(1, Math.floor(params.itemQty / 2)));
                }
            }
        }
    ];

    var EVENT_DEF_MAP = {};
    var i;
    for (i = 0; i < EVENT_DEFS.length; i++) EVENT_DEF_MAP[EVENT_DEFS[i].id] = EVENT_DEFS[i];

    function _findActiveIndex(id) {
        var i;
        for (i = 0; i < player._activeUnsolicitedEvents.length; i++) {
            if (player._activeUnsolicitedEvents[i] && player._activeUnsolicitedEvents[i].id === id) return i;
        }
        return -1;
    }
    function _surfaceReadyActive() {
        var day = _getDay();
        var i, inst;
        if (player._pendingUnsolicitedEvent) return true;
        for (i = 0; i < player._activeUnsolicitedEvents.length; i++) {
            inst = player._activeUnsolicitedEvents[i];
            if (!inst || inst.status !== 'ready') continue;
            if (inst.dueDay > day) continue;
            if (inst.params && inst.params.dismissedDay === day) continue;
            player._pendingUnsolicitedEvent = _copy(inst);
            return true;
        }
        return false;
    }
    function _eligibleDefs(ctx) {
        var defs = [];
        var i, def, last, catLast;
        for (i = 0; i < EVENT_DEFS.length; i++) {
            def = EVENT_DEFS[i];
            last = player._unsolicitedEventCooldowns[def.id];
            catLast = player._unsolicitedEventCatCooldowns[def.category];
            if (last != null && (ctx.day - last) < PER_EVENT_COOLDOWN_DAYS) continue;
            if (catLast != null && (ctx.day - catLast) < (CATEGORY_COOLDOWNS[def.category] || 0)) continue;
            if (MULTI_TEMPLATES[def.template] && ctx.activeMulti >= MAX_ACTIVE_MULTI_STEP) continue;
            try {
                if (def.condition(ctx)) defs.push(def);
            } catch (e) {}
        }
        return defs;
    }
    function _weightedPick(defs, rng) {
        var total = 0;
        var i, roll;
        for (i = 0; i < defs.length; i++) total += defs[i].weight || 1;
        if (!total) return null;
        roll = rng.random() * total;
        for (i = 0; i < defs.length; i++) {
            roll -= defs[i].weight || 1;
            if (roll <= 0) return defs[i];
        }
        return defs.length ? defs[defs.length - 1] : null;
    }
    function _setTriggered(def, instance, day) {
        player._lastUnsolicitedEventDay = day;
        player._unsolicitedEventCooldowns[def.id] = day;
        player._unsolicitedEventCatCooldowns[def.category] = day;
        if (MULTI_TEMPLATES[def.template]) {
            instance.status = 'ready';
            player._activeUnsolicitedEvents.push(_copy(instance));
        }
        player._pendingUnsolicitedEvent = _copy(instance);
        _log(def.icon + ' ' + def.title + ' has found ' + _playerName() + '.', { type: 'unsolicited_event', defId: def.id }, 'my_actions');
    }
    function _tryGenerate(chance) {
        var ctx, defs, pool, def, instance;
        if (!_ensureState()) return false;
        tickUnsolicitedEvents();
        if (_suppressed() || player._pendingUnsolicitedEvent) return false;
        ctx = _buildContext();
        if (!ctx.rng || !ctx.town) return false;
        if ((ctx.day - player._lastUnsolicitedEventDay) < GLOBAL_COOLDOWN_DAYS) return false;
        if (!ctx.rng.chance(chance)) return false;
        defs = _eligibleDefs(ctx);
        pool = defs.slice();
        while (pool.length) {
            def = _weightedPick(pool, ctx.rng);
            if (!def) break;
            try { instance = def.generate(ctx); } catch (e) { instance = null; }
            if (instance) {
                _setTriggered(def, instance, ctx.day);
                return true;
            }
            pool.splice(pool.indexOf(def), 1);
        }
        return false;
    }

    function tryGenerateDailyUnsolicitedEvent() {
        var ch = window._godUeBoost ? 0.50 : DAILY_CHANCE;
        return _tryGenerate(ch);
    }
    // God mode: force-fire an event, bypassing cooldowns and RNG
    function _godForceUnsolicitedEvent() {
        var ctx, defs, pool, def, instance;
        if (!_ensureState()) return { success: false, reason: 'no player state' };
        if (player._pendingUnsolicitedEvent) return { success: false, reason: 'pending event already exists (dismiss it first)' };
        ctx = _buildContext();
        if (!ctx.rng) return { success: false, reason: 'no RNG available' };
        if (!ctx.town) return { success: false, reason: 'player not in a town (townId=' + (player.townId || 'null') + ')' };
        defs = _eligibleDefs(ctx);
        if (!defs.length) return { success: false, reason: 'no eligible events (all on cooldown or conditions not met)' };
        pool = defs.slice();
        while (pool.length) {
            def = _weightedPick(pool, ctx.rng);
            if (!def) break;
            try { instance = def.generate(ctx); } catch (e) { instance = null; }
            if (instance) {
                _setTriggered(def, instance, ctx.day);
                return { success: true, reason: 'fired: ' + def.title + ' (' + def.id + ')' };
            }
            pool.splice(pool.indexOf(def), 1);
        }
        return { success: false, reason: defs.length + ' eligible defs but all generate() returned null' };
    }
    // God mode: diagnostic info
    function _godDiagUnsolicitedEvents() {
        if (!_ensureState()) return { state: false };
        var ctx = _buildContext();
        var suppressed = _suppressed();
        var hasPending = !!player._pendingUnsolicitedEvent;
        var hasTown = !!(ctx && ctx.town);
        var hasRng = !!(ctx && ctx.rng);
        var day = _getDay();
        var cooldownOk = (day - player._lastUnsolicitedEventDay) >= GLOBAL_COOLDOWN_DAYS;
        var eligible = 0;
        try { eligible = _eligibleDefs(ctx).length; } catch(e) {}
        return {
            state: true, day: day, suppressed: suppressed, hasPending: hasPending,
            hasTown: hasTown, townId: player.townId || null,
            hasRng: hasRng, cooldownOk: cooldownOk,
            lastEventDay: player._lastUnsolicitedEventDay,
            globalCooldown: GLOBAL_COOLDOWN_DAYS,
            eligibleDefs: eligible,
            totalDefs: EVENT_DEFS.length,
            activeMulti: (player._activeUnsolicitedEvents || []).length,
            boosted: !!window._godUeBoost
        };
    }
    function tryGenerateEntryUnsolicitedEvent(townId) {
        var day;
        if (!_ensureState()) return false;
        if (townId && player.townId && townId !== player.townId) return false;
        day = _getDay();
        if (player._lastUnsolicitedEventEntryDay === day) return false;
        player._lastUnsolicitedEventEntryDay = day;
        return _tryGenerate(ENTRY_CHANCE);
    }

    function tickUnsolicitedEvents() {
        var day, i, inst, def, pendingIdx;
        if (!_ensureState()) return [];
        day = _getDay();
        for (i = player._activeUnsolicitedEvents.length - 1; i >= 0; i--) {
            inst = player._activeUnsolicitedEvents[i];
            def = inst ? EVENT_DEF_MAP[inst.defId] : null;
            if (!inst || !def || (day - inst.generatedDay) > ACTIVE_EXPIRY_DAYS) {
                if (inst && def) _log((def.icon || '⏳') + ' ' + (def.title || inst.defId) + ' expired before it could conclude.', { type: 'unsolicited_event_expired', defId: inst.defId }, 'my_actions');
                player._activeUnsolicitedEvents.splice(i, 1);
                continue;
            }
            if (inst.npcId) {
                var npc = _findPerson(inst.npcId);
                if (npc && npc.alive === false) {
                    _log((def.icon || '💀') + ' ' + (def.title || inst.defId) + ' ends because a key figure is gone.', { type: 'unsolicited_event_ended', defId: inst.defId }, 'my_actions');
                    player._activeUnsolicitedEvents.splice(i, 1);
                    continue;
                }
            }
            if (inst.status === 'waiting' && inst.dueDay <= day) inst.status = 'ready';
        }
        if (player._pendingUnsolicitedEvent) {
            if ((day - player._pendingUnsolicitedEvent.generatedDay) > ACTIVE_EXPIRY_DAYS || !EVENT_DEF_MAP[player._pendingUnsolicitedEvent.defId]) {
                player._pendingUnsolicitedEvent = null;
            } else if (MULTI_TEMPLATES[(EVENT_DEF_MAP[player._pendingUnsolicitedEvent.defId] || {}).template]) {
                pendingIdx = _findActiveIndex(player._pendingUnsolicitedEvent.id);
                if (pendingIdx < 0) player._pendingUnsolicitedEvent = null;
            }
        }
        _surfaceReadyActive();
        return getActiveUnsolicitedEvents();
    }

    function getPendingUnsolicitedEvent() {
        var ctx, inst, def, raw, out, i, reason;
        if (!_ensureState()) return null;
        _surfaceReadyActive();
        inst = player._pendingUnsolicitedEvent;
        if (!inst) return null;
        def = EVENT_DEF_MAP[inst.defId];
        if (!def) return null;
        ctx = _buildContext();
        raw = _rawStep(def, inst, ctx);
        out = {
            instanceId: inst.id,
            defId: def.id,
            stepIndex: inst.stepIndex,
            title: _fill(raw.title || def.title, inst.params, ctx),
            icon: raw.icon || def.icon,
            text: _fill(raw.text, inst.params, ctx),
            choices: []
        };
        for (i = 0; i < raw.choices.length; i++) {
            reason = _requireReason(raw.choices[i], inst.params);
            out.choices.push({
                id: raw.choices[i].id,
                label: _fill(raw.choices[i].label, inst.params, ctx),
                disabled: !!reason,
                disabledReason: reason || null
            });
        }
        return out;
    }

    function getActiveUnsolicitedEvents() {
        var list = [];
        var i, inst, def, ctx, raw;
        if (!_ensureState()) return list;
        ctx = _buildContext();
        for (i = 0; i < player._activeUnsolicitedEvents.length; i++) {
            inst = player._activeUnsolicitedEvents[i];
            def = inst ? EVENT_DEF_MAP[inst.defId] : null;
            if (!inst || !def) continue;
            raw = _rawStep(def, inst, ctx);
            list.push({
                id: inst.id,
                defId: inst.defId,
                title: def.title,
                icon: def.icon,
                category: def.category,
                stepIndex: inst.stepIndex,
                status: inst.status,
                dueDay: inst.dueDay,
                generatedDay: inst.generatedDay,
                townId: inst.townId,
                kingdomId: inst.kingdomId,
                npcId: inst.npcId,
                summary: _fill(raw.text, inst.params, ctx)
            });
        }
        return list;
    }

    function dismissPendingUnsolicitedEvent() {
        var inst, idx, day;
        if (!_ensureState()) return false;
        inst = player._pendingUnsolicitedEvent;
        if (!inst) return false;
        day = _getDay();
        idx = _findActiveIndex(inst.id);
        if (idx >= 0) {
            player._activeUnsolicitedEvents[idx].status = 'waiting';
            player._activeUnsolicitedEvents[idx].dueDay = day + 1;
            player._activeUnsolicitedEvents[idx].params.dismissedDay = day;
        }
        player._pendingUnsolicitedEvent = null;
        return true;
    }

    function handleUnsolicitedEventChoice(eventInstanceId, choiceIndex) {
        var inst, def, ctx, raw, choice, i, idx, delay, nextCopy, choiceId, reason;
        if (!_ensureState()) return { success: false, message: 'No player state.' };
        inst = player._pendingUnsolicitedEvent;
        if (!inst) return { success: false, message: 'No pending unsolicited event.' };
        if (typeof choiceIndex === 'undefined') {
            choiceId = eventInstanceId;
            eventInstanceId = inst.id;
        } else if (typeof choiceIndex === 'number') {
            if (choiceIndex >= 0) choice = null;
        } else {
            choiceId = choiceIndex;
        }
        if (eventInstanceId && inst.id !== eventInstanceId) return { success: false, message: 'That event is no longer pending.' };
        def = EVENT_DEF_MAP[inst.defId];
        if (!def) {
            player._pendingUnsolicitedEvent = null;
            return { success: false, message: 'Event definition missing.' };
        }
        ctx = _buildContext();
        raw = _rawStep(def, inst, ctx);
        if (typeof choiceIndex === 'number') {
            choice = raw.choices[choiceIndex] || null;
        } else {
            for (i = 0; i < raw.choices.length; i++) {
                if (raw.choices[i].id === choiceId) { choice = raw.choices[i]; break; }
            }
        }
        if (!choice) return { success: false, message: 'Choice not found.' };
        reason = _requireReason(choice, inst.params);
        if (reason) return { success: false, message: reason };
        inst.choiceHistory.push({ day: ctx.day, stepIndex: inst.stepIndex, choiceId: choice.id });
        inst.params.kingdomId = inst.kingdomId;
        inst.params.npcId = inst.npcId;
        try { def.applyEffect(choice.effectKey, inst.params, ctx); } catch (e) {}
        idx = _findActiveIndex(inst.id);
        if (choice.nextStepIndex == null) {
            if (idx >= 0) player._activeUnsolicitedEvents.splice(idx, 1);
            player._pendingUnsolicitedEvent = null;
            _log(def.icon + ' ' + def.title + ' resolves for ' + _playerName() + '.', { type: 'unsolicited_event_resolved', defId: def.id }, 'my_actions');
            _surfaceReadyActive();
            return { success: true, message: 'Choice resolved.' };
        }
        inst.stepIndex = choice.nextStepIndex;
        delay = _stepDelay(def, inst, choice.nextStepIndex);
        if (idx >= 0) {
            player._activeUnsolicitedEvents[idx] = _copy(inst);
            player._activeUnsolicitedEvents[idx].status = delay > 0 ? 'waiting' : 'ready';
            player._activeUnsolicitedEvents[idx].dueDay = ctx.day + delay;
            nextCopy = _copy(player._activeUnsolicitedEvents[idx]);
        } else {
            nextCopy = _copy(inst);
            nextCopy.status = delay > 0 ? 'waiting' : 'ready';
            nextCopy.dueDay = ctx.day + delay;
        }
        player._pendingUnsolicitedEvent = null;
        if (delay > 0) {
            _log(def.icon + ' ' + def.title + ' will return in ' + delay + ' days.', { type: 'unsolicited_event_waiting', defId: def.id }, 'my_actions');
        } else {
            player._pendingUnsolicitedEvent = _copy(nextCopy);
        }
        return { success: true, message: delay > 0 ? 'Choice made. The story continues later.' : 'Choice made.' };
    }

    Player.tryGenerateDailyUnsolicitedEvent = tryGenerateDailyUnsolicitedEvent;
    Player.tryGenerateEntryUnsolicitedEvent = tryGenerateEntryUnsolicitedEvent;
    Player.tickUnsolicitedEvents = tickUnsolicitedEvents;
    Player.getPendingUnsolicitedEvent = getPendingUnsolicitedEvent;
    Player.handleUnsolicitedEventChoice = handleUnsolicitedEventChoice;
    Player.getActiveUnsolicitedEvents = getActiveUnsolicitedEvents;
    Player.dismissPendingUnsolicitedEvent = dismissPendingUnsolicitedEvent;
    Player._godForceUnsolicitedEvent = _godForceUnsolicitedEvent;
    Player._godDiagUnsolicitedEvents = _godDiagUnsolicitedEvents;
})(window.Player);
